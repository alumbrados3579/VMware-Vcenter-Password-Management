# VMware vCenter Password Management Tool - Git LFS Configuration
# ONLY .zip files should be in LFS for module distribution

# LFS for large binary files only
*.zip filter=lfs diff=lfs merge=lfs -text

# Everything else should be regular git files for GitHub web accessibility
# This ensures scripts can be viewed and copied directly from GitHub