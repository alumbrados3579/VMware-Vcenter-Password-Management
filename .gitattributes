# VMware vCenter Password Management Tool - Git Configuration
# Only large binary files should use LFS

# LFS for large binary files only (if needed in future)
*.zip filter=lfs diff=lfs merge=lfs -text

# All scripts and text files are regular git files for GitHub web accessibility